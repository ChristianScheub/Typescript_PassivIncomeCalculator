/**
 * Analytics recommendations and insights
 */

// Recommendation types werden jetzt zentral in reporting.ts gepflegt
// export type RecommendationPriority = 'high' | 'medium' | 'low';
// export type RecommendationCategory = 'assets' | 'income' | 'expenses' | 'liabilities' | 'planning';

// Financial metrics für analysis werden jetzt zentral in reporting.ts gepflegt
// export interface FinancialMetrics { ... }

// recommendations.ts enthält nur noch Recommendation-spezifische Typen
// (ggf. weitere Recommendation-Interfaces hier definieren)
