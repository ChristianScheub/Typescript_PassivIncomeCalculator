import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.passiveincome.calculator',
  appName: 'PassiveIncomeCalculator',
  webDir: 'dist'
};

export default config;
