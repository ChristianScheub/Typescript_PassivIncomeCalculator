import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.passiveincome.calculator',
  appName: 'StrictFinance',
  webDir: 'dist'
};

export default config;
